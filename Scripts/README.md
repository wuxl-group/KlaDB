# Scripts

- `motif_analysis/` regenerates the motif dataset and performs sequence-logo or background-enrichment analyses.
- `benchmark_preparation/` documents leakage filtering and recalculates the deterministic resampling summaries.

Python 3.9 or later is recommended. Each subdirectory contains its own usage notes and dependency list.
